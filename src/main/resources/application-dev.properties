logging.level.org.springframework.security=DEBUG
# WARNING: Debug logging may expose sensitive information. Only use in development.

spring.data.mongodb.host=${MONGO_HOST:localhost}
spring.data.mongodb.port=${MONGO_PORT:27017}
spring.data.mongodb.database=${MONGO_DATABASE:test}

# Encryption key must be set via ENCRYPTION_KEY environment variable

# Dev-only JWT fallback so local bootRun works without env setup.
# Never used outside the dev profile; default/prod profiles fail fast without JWT_SECRET.
jwt.secret=${JWT_SECRET:localDevelopmentOnlySecretDoNotUseInProduction1234}